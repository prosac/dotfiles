# Drop-in, not a fork: grub-boot-success.service is shipped by grub2-tools, so
# only the OnFailure hook is added here and vendor updates keep applying.
# %n expands to this unit's full name, giving notify-failure@grub-boot-success.service.
[Unit]
OnFailure=notify-failure@%n.service
