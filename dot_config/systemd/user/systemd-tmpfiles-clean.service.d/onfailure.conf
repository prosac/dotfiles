# Drop-in, not a fork: systemd-tmpfiles-clean.service is shipped by systemd.
# Worth watching — this is the same tmpfiles machinery that recreates
# /home/.snapshots for dotsnap (see dotsnap-btrfs-snapshots.md).
[Unit]
OnFailure=notify-failure@%n.service
