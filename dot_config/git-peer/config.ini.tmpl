{{/* Rendered by chezmoi. The values come from ~/.config/chezmoi/chezmoi.toml,
     which is generated by `chezmoi init` and is NOT in this repo -- the repo is
     public, and a LAN CIDR plus internal hostnames are private topology.
     Consumed by ~/.local/bin/git-peer and ~/.local/bin/git-peer-bootstrap. */ -}}
# Managed by chezmoi -- edit via `chezmoi init` (re-prompts) or
# `chezmoi edit ~/.config/git-peer/config.ini`.
[peer]
# The OTHER machine. Its first label doubles as the ssh alias.
host = {{ .gitPeerHost }}
# Only this CIDR may reach sshd.
lan = {{ .gitPeerLan }}
